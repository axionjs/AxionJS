---
title: Limited Input
description: A customizable input field with character limit and validation support.
---

import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { TypeTable } from "fumadocs-ui/components/type-table";
import { Files, Folder, File } from "fumadocs-ui/components/files";
import {
  SimpleLimitedInputPreview,
  ValidatedLimitedInputPreview,
} from "@/app/components/docs/LimitedInputPreview";

## Previews with Code

### Example 1: Simple Limited Input

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview" className="m-auto h-auto pb-8 content-center">
    <SimpleLimitedInputPreview />
  </Tab>
  <Tab value="Code" className="w-auto h-auto">
    ```tsx ts2js
    export function SimpleLimitedInputPreview() {
      const [value, setValue] = useState("");

      return (
        <div className="space-y-4">
          <h3 className="text-lg font-medium">Simple Limited Input</h3>
          <LimitedInput
            value={value}
            onChange={setValue}
            characterLimit={20}
            placeholder="Type something..."
          />
        </div>
      );
    }
    ```
  </Tab>
</Tabs>

### Example 2: Validated Limited Input

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview" className="m-auto h-auto pb-8 content-center">
    <ValidatedLimitedInputPreview />
  </Tab>
  <Tab value="Code" className="w-auto h-auto">
    ```tsx ts2js
    export function ValidatedLimitedInputPreview() {
      const [value, setValue] = useState("");
      const isValid = value.length >= 5;
      const error = !isValid ? "Input must be at least 5 characters long." : undefined;

      return (
        <div className="space-y-4">
          <h3 className="text-lg font-medium">Validated Limited Input</h3>
          <LimitedInput
            label="Validated Input"
            helperText="Please enter at least 5 characters."
            value={value}
            onChange={setValue}
            characterLimit={30}
            required
            error={error}
            placeholder="Enter your text here..."
          />
        </div>
      );
    }
    ```
  </Tab>
</Tabs>

---

## Installation

The `LimitedInput` component provides a user-friendly input field with character limit and validation capabilities.

<Tabs items={["CLI", "Manual"]}>
  <Tab value="CLI">
    Quickly add the component using:
    ```bash
    npx axionjs add limited-input
    ```
  </Tab>
  <Tab value="Manual">
    ### 1. Add the Component

    Create a `limited-input.tsx` file in your `components/ui` directory:
    ```tsx
    // Full LimitedInput component code provided in the query
    ```

    ### 2. Add Preview Components

    Create a `LimitedInputPreview.tsx` file for demonstration purposes:
    ```tsx
    // Full LimitedInputPreview.tsx code provided above
    ```
  </Tab>
</Tabs>

---

## File Structure

<Files>
  <Folder name="app" defaultOpen>
    <Folder name="components">
      <Folder name="ui">
        <File name="limited-input.tsx" />
      </Folder>
    </Folder>
  </Folder>
</Files>

---

## Props

### LimitedInput Props

<TypeTable
  type={{
    label: {
      description: "The label for the input field.",
      type: "string",
      default: `"Input Label"`,
    },
    id: {
      description: "The unique identifier for the input.",
      type: "string",
      default: `"limited-input"`,
    },
    value: {
      description: "The value of the input field.",
      type: "string",
      required: true,
    },
    onChange: {
      description: "Callback triggered when the input value changes.",
      type: "(value: string) => void",
      required: true,
    },
    characterLimit: {
      description: "The maximum number of characters allowed.",
      type: "number",
      default: `50`,
    },
    required: {
      description: "Marks the input as required.",
      type: "boolean",
      default: `false`,
    },
    helperText: {
      description: "Additional helper text displayed above the input.",
      type: "string",
      required: false,
    },
    error: {
      description: "Error message displayed below the input.",
      type: "string",
      required: false,
    },
    placeholder: {
      description: "Placeholder text for the input field.",
      type: "string",
      required: false,
    },
  }}
/>

---

## Features

- **Character Limit**:
  - Restricts input to a specific number of characters.
- **Validation**:
  - Supports custom error messages and helper text.
- **Customizable**:
  - Allows flexible styling and behavior through props.
- **Responsive**:
  - Dynamically updates remaining character count.

---

## Accessibility

The `LimitedInput` component includes accessible labels, ARIA attributes, and dynamic feedback for users. Ensure that error messages and helper text are descriptive for screen readers.

---

## Conclusion

The `LimitedInput` component is an essential tool for creating input fields with character limits and validation, improving user experience and ensuring consistent data entry.
